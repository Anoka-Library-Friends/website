{
  "name": "anoka-library-friends",
  "version": "1.0.0",
  "description": "Static website for Friends of the Anoka County Library nonprofit",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test scripts/parse-markdown.test.js",
    "test:e2e": "npm run build && playwright test",
    "test:all": "npm test && npm run build && playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.9.0",
    "@playwright/test": "^1.44.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
